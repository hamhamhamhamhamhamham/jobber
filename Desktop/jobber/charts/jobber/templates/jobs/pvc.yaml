apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: uploads-pvc #🎁 pvc supports r-w multiple job pods running + delete,then creation 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi  