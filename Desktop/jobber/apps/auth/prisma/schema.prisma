// **NOTE** : clients not client
// **NOTE** > + special value for microservices to avoid overriding each other

// **PRISMA TYPESCRIPT**
generator client {
    provider = "prisma-client-js"
    binaryTargets = ["native","debian-openssl-1.1.x"] // local dev: native , 2nd: docker containers in run time
    output   = "../../../node_modules/@prisma-clients/auth"
}

//CONNECTION string to : POSTGRES on DOCKER(out of kubernetes(minikube))
datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

// Table schema 
model User {
    id       Int    @id @default(autoincrement())
    email    String @unique
    password String
}
